<Window x:Class="ScreenPresenterAssist.ToolbarWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Screen Presenter"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        Topmost="True"
        SizeToContent="WidthAndHeight"
        ShowInTaskbar="False"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">
    
    <Window.Resources>
        <!-- ボタンスタイル -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Width" Value="44"/>
            <Setter Property="Height" Value="44"/>
            <Setter Property="Margin" Value="4"/>
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#4A4A6A"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                CornerRadius="10"
                                BorderThickness="0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#5DADE2"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#3498DB"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- OFFボタン用スタイル -->
        <Style x:Key="OffButton" TargetType="Button">
            <Setter Property="Width" Value="44"/>
            <Setter Property="Height" Value="44"/>
            <Setter Property="Margin" Value="4"/>
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#E74C3C"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                CornerRadius="10"
                                BorderThickness="0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#F1948A"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#C0392B"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- カラーボタンスタイル -->
        <Style x:Key="ColorButton" TargetType="Button">
            <Setter Property="Width" Value="20"/>
            <Setter Property="Height" Value="34"/>
            <Setter Property="Margin" Value="2,2"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                CornerRadius="10"
                                BorderThickness="1"
                                BorderBrush="Transparent">
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="White"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#FFD700"/>
                                <Setter TargetName="border" Property="Opacity" Value="0.8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <!-- メインコンテナ -->
    <Border CornerRadius="16" Padding="8" Background="#2A2A4A">
        <StackPanel Orientation="Horizontal">
            <!-- モード切替ボタン -->
            <Button x:Name="BtnDraw" Content="✏️" Style="{StaticResource ModernButton}" ToolTip="描画モード (Ctrl+Alt+P)" Click="BtnDraw_Click"/>
            <Button x:Name="BtnHighlight" Content="🔦" Style="{StaticResource ModernButton}" ToolTip="強調表示 (Ctrl+Alt+H)" Click="BtnHighlight_Click"/>
            <Button x:Name="BtnMagnify" Content="🔍" Style="{StaticResource ModernButton}" ToolTip="拡大鏡 (Ctrl+Alt+Z)" Click="BtnMagnify_Click"/>
            <Button x:Name="BtnClear" Content="🗑️" Style="{StaticResource ModernButton}" ToolTip="全消去 (Ctrl+Alt+X)" Click="BtnClear_Click"/>
            <Button x:Name="BtnOff" Content="⏹️" Style="{StaticResource OffButton}" ToolTip="機能OFF (Esc)" Click="BtnOff_Click"/>
            
            <!-- 色選択パネル（描画モード時のみ表示） -->
            <StackPanel x:Name="ColorPanel" Orientation="Horizontal" Visibility="Collapsed">
                <!-- 区切り線 -->
                <Border Width="2" Background="#4A4A6A" Margin="4,8" CornerRadius="1"/>
                
                <!-- 色選択ボタン -->
                <Button x:Name="BtnRed" Background="#FF6B6B" Style="{StaticResource ColorButton}" ToolTip="赤 (1)" Click="BtnRed_Click"/>
                <Button x:Name="BtnBlue" Background="#74B9FF" Style="{StaticResource ColorButton}" ToolTip="青 (2)" Click="BtnBlue_Click"/>
                <Button x:Name="BtnYellow" Background="#FDCB6E" Style="{StaticResource ColorButton}" ToolTip="黄 (3)" Click="BtnYellow_Click"/>
                <Button x:Name="BtnGreen" Background="#27AE60" Style="{StaticResource ColorButton}" ToolTip="緑 (4)" Click="BtnGreen_Click"/>
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
